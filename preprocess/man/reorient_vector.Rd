% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reorient_vector.R
\name{reorient_vector}
\alias{reorient_vector}
\alias{\%orientbc<-\%}
\title{Change Orientation of An Oriented Vector and Make it a Broadcaster}
\usage{
lhs \%orientbc<-\% rhs
}
\arguments{
\item{lhs}{an atomic or recursive oriented vector.}

\item{rhs}{an integer vector. \cr
The first value must be the new orientation,
referred to as \code{orient}. \cr
The second value, optional,
must be the number of dimensions, referred to as \code{ndim}. \cr
If no second value is given, it will be set at \code{max(ndim(lhs), rhs[1])}. \cr
\cr}
}
\value{
Nothing, but changes \code{lhs} directly. \cr
\cr
}
\description{
The \code{lhs \%orientbc<-\% rhs} operator
takes an oriented vector (\code{lhs}),
and changes its orientation. \cr
It also makes \code{lhs} a broadcaster if it wasn't already. \cr
\cr
The modification is done directly on \code{lhs} itself,
using R's native semantics. \cr
\cr
}
\details{
In the context of broadcasting,
an oriented vector \code{x} is a vector or array that satisfies the following conditions:
\itemize{
\item It has length \verb{> 1} and \verb{<= (2^31 - 1)};
\item It has no dimensions,
or one dimension is \code{length(x)} and all other dimensions are \code{1}. \cr
}

The orientation of an oriented vector is the index of the dimension with size > 1. \cr
For example, if array \code{lhs} has dimensions \code{c(1, 10, 1)},
its orientation is \code{2L},
since the second dimension has size > 1. \cr
A vector with no dimensions has orientation \code{1L}. \cr
\cr
In the context of broadcasted operations,
oriented vectors can be used to modify an array along a dimension. \cr
The examples section gives several examples of this. \cr
\cr
}
\examples{


# basic usage ====
x <- setNames(1:10, letters[1:10])
print(x)

x \%orientbc<-\% 2L
print(x)
x \%orientbc<-\% 3L
print(x)
x \%orientbc<-\% 1:2
print(x)
x \%orientbc<-\% c(1, 3)
print(x)



# modify an array along one specific dimension ====
x <- array(sample(0:9), c(3,3,3))
print(x)

# add 10 to second column, and 100 to third column:
v <- vector2array(c(0, 10, 100), 2, 2, TRUE)
x + v

# add 10 to second row, and 100 to third row:
v \%orientbc<-\% 1L
x + v

# add 10 to second layer, and 100 to third layer:
v \%orientbc<-\% 3L
x + v

}
\seealso{
\code{[broadcast_casting](aaa02_broadcast_casting.qmd)} \cr
\cr
}
