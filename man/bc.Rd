% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bc.R
\name{bc}
\alias{bc}
\title{Broadcasted Relational Operations on Atomic Arrays}
\usage{
bc(x, y, op, prec = sqrt(.Machine$double.eps))
}
\arguments{
\item{x, y}{conformable atomic arrays of types.}

\item{op}{a single string, giving the operator. \cr
Supported operators: ==, !=, <, >, <=, >=.}

\item{prec}{a single decimal number, giving the machine precision. \cr
Only applicable when \code{x} and/or \code{y} is of type \code{double}}
}
\value{
A numeric array as a result of the broadcasted arithmeric operation. \cr
}
\description{
The \code{bc()} function performs broadcasted relational operations on 2 atomic arrays. \cr
}
\examples{
x.dim <- c(10:8)
x.len <- prod(x.dim)
x.data <- sample(c(NA, 1.1:1000.1), x.len, TRUE)
x <- array(x.data, x.dim)
y <- array(1:50, c(10,1,1))

bc(x, y, "==")
bc(x, y, "!=")
bc(x, y, "<")
bc(x, y, ">")
bc(x, y, "<=")
bc(x, y, ">=")
}
