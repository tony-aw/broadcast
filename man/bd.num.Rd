% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bc_num.R
\name{bc.num}
\alias{bc.num}
\title{Broadcasted Operations for Numeric Arrays}
\usage{
bc.num(x, y, op, prec = sqrt(.Machine$double.eps))
}
\arguments{
\item{x, y}{conformable atomic arrays of types \code{logical}, \code{integer}, or \code{double}.}

\item{op}{a single string, giving the operator. \cr
Supported arithmetic operators: +, -, *, /, ^, intmod, pmin, pmax. \cr
Supported relational operators: ==, !=, <, >, <=, >=, d==, d!=, d<, d>, d<=, d>=. \cr}

\item{prec}{a single number between 0 and 0.1, giving the machine precision to use. \cr
Only relevant for the following operators: \cr
d==, d!=, d<, d>, d<=, d>= \cr
See the
d==, d!=, d<, d>, d<=, d>= operators
from the 'tinycodet' package for details. \cr}
}
\value{
For arithmetic operators: \cr
A numeric array as a result of the broadcasted arithmetic operation. \cr
\cr
For relational operators: \cr
A logical array as a result of the broadcasted relational comparison. \cr
\cr
}
\description{
The \code{bc.num()} function performs broadcasted operations on 2 numeric arrays. \cr
}
\examples{
x.dim <- c(10:8)
x.len <- prod(x.dim)
x.data <- sample(c(NA, 1.1:1000.1), x.len, TRUE)
x <- array(x.data, x.dim)
y <- array(1:50, c(10,1,1))

bc.num(x, y, "+")
bc.num(x, y, "-")
bc.num(x, y, "*")
bc.num(x, y, "/")
bc.num(x, y, "^")

bc.num(x, y, "==")
bc.num(x, y, "!=")
bc.num(x, y, "<")
bc.num(x, y, ">")
bc.num(x, y, "<=")
bc.num(x, y, ">=")
}
