% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain.R
\name{bc}
\alias{bc}
\title{Evaluate a Chain of Mathematical Operations with Broadcasting}
\usage{
bc(f)
}
\arguments{
\item{f}{a formula giving the expression to evaluate. \cr
The environment of \code{f} is used to find the variables. \cr
If \code{environment(f)} is \code{NULL},
the environment from which \code{bc()} was called
is used to find the variables. \cr \cr}
}
\value{
The result from the broadcasted operation. \cr
}
\description{
The user can supply a formula giving a mathematical (-like) expression in the \code{bc()} function. \cr
Inside \code{bc()}, the expression is evaluated,
where the base operators
(+, -, *, /, etc.)
have been \bold{overloaded} to use \bold{broadcasting}. \cr
I.e. \code{bc(~ x + y)} is equivalent to \code{bc.num(x, y, "+")}. \cr
What sets \code{bc()} apart from the other \code{bc.}-functions,
other than the different syntax,
is that the user can chain together multiple arithmetic operators,
\bold{with the usual mathematical precedence and backeting rules}. \cr
For example: \cr
\code{bc(~ (x + y) / z)} \cr
\cr
Currently, all mathematical operators and all Boolean operators are supported,
but relational operators have not yet been implemented. \cr
\cr
}
\examples{

x <- 1:10
y <- 1:10
bc(~ x + y / x)
x + 1 # mathematically equivalent to the above, since x == y
bc(~ (x + y) / x)
2 * x/x # mathematically equivalent to the above, since x == y

dim(x) <- c(10, 1)
dim(y) <- c(1, 10)

bc(~ x + y / x)
bc(~ (x + y) / x)
bc(~ ifelse(x > 1, y, x)) # ifelse and apply are not overloaded
}
