body <- "
if(ndim <= 2) {
return Rf_ScalarInteger(2);
}
if(ndim <= 4) {
return Rf_ScalarInteger(4);
}
if(ndim <= 8) {
return Rf_ScalarInteger(8);
}
if(ndim <= 12) {
return Rf_ScalarInteger(12);
}
if(ndim <= 16) {
return Rf_ScalarInteger(16);
}
"
body <- "
int ndim = INTEGER(ndim0)[0];
if(ndim <= 2) {
return Rf_ScalarInteger(2);
}
if(ndim <= 4) {
return Rf_ScalarInteger(4);
}
if(ndim <= 8) {
return Rf_ScalarInteger(8);
}
if(ndim <= 12) {
return Rf_ScalarInteger(12);
}
if(ndim <= 16) {
return Rf_ScalarInteger(16);
}
"
C_chunksize <- inline::cfunction(signature(ndim0 = "SEXP"), body)
C_chunksize(3)
C_chunksize(3L)
C_chunksize(2L)
C_chunksize(1L)
C_chunksize(0L)
C_chunksize(15L)
C_chunksize(14L)
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
const int pchunks = INTEGER_RO(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int pdimsold = INTEGER_RO(dimsold);
int pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold)
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
const int pchunks = INTEGER_RO(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int pdimsold = INTEGER_RO(dimsold);
int pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold)
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
int pchunks = INTEGER(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int pdimsold = INTEGER_RO(dimsold);
int pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold)
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
int *pchunks = INTEGER(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int pdimsold = INTEGER_RO(dimsold);
int pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold)
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
int *pchunks = INTEGER(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int *pdimsold = INTEGER_RO(dimsold);
int *pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold)
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp/Lightest>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_size)]]
int rcpp_chunk_size(int ndim, SEXP chunks) {
int *pchunks = INTEGER(chunks);
for(int i = 0; i < Rf_length(chunks); ++i) {
if(ndim <= pchunks[i]) {
return pchunks[i];
}
}
return 0;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(.rcpp_chunk_set)]]
void rcpp_chunk_set(SEXP dimsold, SEXP dimsnew) {
const int *pdimsold = INTEGER_RO(dimsold);
int *pdimsnew = INTEGER(dimsnew);
int n = Rf_length(dimsold);
for(int i = 0; i < n; ++i) {
pdimsnew[i] = pdimsold[i];
}
}
"
Rcpp::sourceCpp(code = code)
chunks <- c(2L, 4L, 8L, 12L, 16L)
.rcpp_chunk_size(15L, chunks)
.rcpp_chunk_size(1L, chunks)
.rcpp_chunk_size(2L, chunks)
.rcpp_chunk_size(3L, chunks)
.rcpp_chunk_size(4L, chunks)
.rcpp_chunk_size(5L, chunks)
old <- sample(2:4)
old <- sample(2:4)
new <- rep(1L, 4L)
.rcpp_chunk_set(old, new)
print(new)
