check = FALSE,
min_iterations = 10,
)
summary(foo)
ggplot2::autoplot(foo)
broadcast:::.binary_prep(a, b, sys.call()),
foo <- bench::mark(
bc = for (i in 1:100) {
bc.num(a, b, "+")
},
internal_main = for(i in 1:100) {
broadcast:::.rcpp_bc_dec_d(a, b, by_a, by_b, dcp_a, dcp_b, out.dim, out.len, 1L)
},
internal_prep = for(i in 1:100) {
broadcast:::.binary_prep(a, b, sys.call()),
foo <- bench::mark(
bc = for (i in 1:100) {
bc.num(a, b, "+")
},
internal_main = for(i in 1:100) {
broadcast:::.rcpp_bc_dec_d(a, b, by_a, by_b, dcp_a, dcp_b, out.dim, out.len, 1L)
},
internal_prep = for(i in 1:100) {
broadcast:::.binary_prep(a, b, sys.call())
},
`numpy (no conversion to R)` = for(i in 1:100) {
npa + npb
},
check = FALSE,
min_iterations = 10,
)
summary(foo)
ggplot2::autoplot(foo)
a.dim <- dim(a)
b.dim <- dim(b)
},
broadcast:::.binary_prep(a, b, sys.call())
foo <- bench::mark(
broadcast:::.binary_simplify_dims(dim(a), dim(b), length(a), length(b))
foo <- bench::mark(
bc = for (i in 1:100) {
bc.num(a, b, "+")
},
internal_main = for(i in 1:100) {
broadcast:::.rcpp_bc_dec_d(a, b, by_a, by_b, dcp_a, dcp_b, out.dim, out.len, 1L)
},
internal_prep = for(i in 1:100) {
broadcast:::.binary_prep(a, b, sys.call())
},
internal_simplifydims = for(i in 1:100) {
broadcast:::.binary_simplify_dims(dim(a), dim(b), length(a), length(b))
},
`numpy (no conversion to R)` = for(i in 1:100) {
npa + npb
},
check = FALSE,
min_iterations = 10,
)
summary(foo)
ggplot2::autoplot(foo)
foo <- bench::mark(
bc = for (i in 1:100) {
bc.num(a, b, "+")
},
internal_main = for(i in 1:100) {
broadcast:::.rcpp_bc_dec_d(a, b, by_a, by_b, dcp_a, dcp_b, out.dim, out.len, 1L)
},
internal_prep = for(i in 1:100) {
broadcast:::.binary_prep(a, b, sys.call())
},
internal_simplifydims = for(i in 1:100) {
broadcast:::.binary_simplify_dims(dim(a), dim(b), length(a), length(b))
},
internal_mergedims = for(i in 1:100) {
broadcast:::.rcpp_mergedims(
dim(x), dim(y),
broadcast:::.rcpp_is_mergeable_with_prev(dim(a) == 1L, dim(b) == 1L)
)
},
`numpy (no conversion to R)` = for(i in 1:100) {
npa + npb
},
check = FALSE,
min_iterations = 10,
)
broadcast:::.rcpp_mergedims(
dim(a), dim(b),
broadcast:::.rcpp_is_mergeable_with_prev(dim(a) == 1L, dim(b) == 1L)
)
foo <- bench::mark(
bc = for (i in 1:100) {
bc.num(a, b, "+")
},
internal_main = for(i in 1:100) {
broadcast:::.rcpp_bc_dec_d(a, b, by_a, by_b, dcp_a, dcp_b, out.dim, out.len, 1L)
},
internal_prep = for(i in 1:100) {
broadcast:::.binary_prep(a, b, sys.call())
},
internal_simplifydims = for(i in 1:100) {
broadcast:::.binary_simplify_dims(dim(a), dim(b), length(a), length(b))
},
internal_mergedims = for(i in 1:100) {
broadcast:::.rcpp_mergedims(
dim(a), dim(b),
broadcast:::.rcpp_is_mergeable_with_prev(dim(a) == 1L, dim(b) == 1L)
)
},
`numpy (no conversion to R)` = for(i in 1:100) {
npa + npb
},
check = FALSE,
min_iterations = 10,
)
summary(foo)
ggplot2::autoplot(foo)
summary(foo)
# in C:
body <- "
const int *pxdim = INTEGER_RO(xdim);
const int *pydim = INTEGER_RO(ydim);
int n = Rf_length(xdim);
int counter = 0;
for(int i = 0; i < n; ++i) {
if(pxdim == 1 && pydim == 1) {
counter++;
}
}
return asScalarInteger(counter);
"
C_dropdims_count <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP"))
C_dropdims_count <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP"), body)
# in C:
body <- "
const int *pxdim = INTEGER_RO(xdim);
const int *pydim = INTEGER_RO(ydim);
int n = Rf_length(xdim);
int counter = 0;
for(int i = 0; i < n; ++i) {
if(pxdim == 1 && pydim == 1) {
counter++;
}
}
return Rf_ScalarInteger(counter);
"
C_dropdims_count <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP"), body)
# in C:
body <- "
const int *pxdim = INTEGER_RO(xdim);
const int *pydim = INTEGER_RO(ydim);
int n = Rf_length(xdim);
int counter = 0;
for(int i = 0; i < n; ++i) {
if(pxdim[i] == 1 && pydim[i] == 1) {
counter++;
}
}
return Rf_ScalarInteger(counter);
"
C_dropdims_count <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP"), body)
C_dropdims_which <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP", size = "SEXP"), body)
C_dropdims_which <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP", size = "SEXP"), body_which)
body_which <- "
const int *pxdim = INTEGER_RO(xdim);
const int *pydim = INTEGER_RO(ydim);
int n = Rf_length(xdim);
int size0 = INTEGER(size)[0];
SEXP out = PROTECT(Rf_allocVector(INTSXP, size0))
int *pout = INTEGER(out);
int counter = 0;
for(int i = 0; i < n; ++i) {
if(pxdim[i] == 1 && pydim[i] == 1) {
pout[counter] = i + 1;
counter++;
}
}
UNPROTECT(1);
return out;
"
C_dropdims_which <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP", size = "SEXP"), body_which)
body_which <- "
const int *pxdim = INTEGER_RO(xdim);
const int *pydim = INTEGER_RO(ydim);
int n = Rf_length(xdim);
int size0 = INTEGER(size)[0];
SEXP out = PROTECT(Rf_allocVector(INTSXP, size0));
int *pout = INTEGER(out);
int counter = 0;
for(int i = 0; i < n; ++i) {
if(pxdim[i] == 1 && pydim[i] == 1) {
pout[counter] = i + 1;
counter++;
}
}
UNPROTECT(1);
return out;
"
C_dropdims_which <- inline::cfunction(signature(xdim = "SEXP", ydim = "SEXP", size = "SEXP"), body_which)
x.dim <- c(2, 1, 5, 1, 5)
y.dim <- c(5, 1, 3, 2, 1)
C_dropdims_count(x.dim, y.dim)
x.dim <- c(2, 1, 5, 1, 5) |> as.integer()
y.dim <- c(5, 1, 3, 2, 1) |> as.integer()
C_dropdims_count(x.dim, y.dim)
x.dim <- c(2, 1, 5, 1, 5, 1) |> as.integer()
y.dim <- c(5, 1, 3, 2, 1, 1) |> as.integer()
C_dropdims_count(x.dim, y.dim)
n <- C_dropdims_count(x.dim, y.dim)
C_dropdims_which(x.dim, y.dim, n)
myfun <- function(x, ...) {return(list(...))}
myfun(1, 1, 2, 3, 4, , 5)
abind::`afill<-`()
?abind::`afill<-`()
abind::`afill<-`
abind:::`afill<-`.default
abind:::`afill<-.default`
myfun <- function(x, ...) {return(alist(...))}
myfun(1, 1, 2, 3, 4, , 5)
foo <- myfun(1, 1, 2, 3, 4, , 5)
alist(1, 2, 3, 4, , 5)
foo
foo <- alist(1, 2, 3, 4, , 5)
foo[[5]]
foo[[5]] |> is.missing
foo[[5]] |> is.missing()
foo[[5]] |> missing()
missing(foo[[54]])
missing(foo[[5]])
class(foo[[5]])
str(foo[[5]])
class(foo[[1]])
?alist
alist(...)
alist(x = , ... =)
?a
ls(envir = baseenv(), all.names = TRUE)
?ls()
ls.base <- ls(envir = baseenv(), all.names = TRUE)
ls.basefuns <- lsf.str(envir = baseenv())
ls.baseobj <- setdiff(ls.base, ls.basefuns)
ls.baseobj
.Internal
is.function(.Internal)
?ls
?ls.str
ls.basefuns <- ls.str(envir = baseenv(), mode = "function")
ls.baseprim <- ls.str(envir = baseenv(), mode = "Primitive")
ls.baseprim <- ls.str(envir = baseenv(), mode = "primitive")
130/6
21.667/52
gc()
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_testdbl)]]
SEXP rcpp_testdbl(
SEXP x, SEXP y
) {
const int *px = INTEGER_RO(x);
const int *py = INTEGER_RO(y);
R_xlen_t n = Rf_xlength(x);
SEXP out = PROTECT(Rf_allocVector(REALSXP, n));
double *pout;
pout = REAL(out);
for(R_xlen_t i = 0; i < n; ++i) {
pout[i] = (double)px[i] + (double)py[i];
}
UNPROTECT(1);
return out;
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_testint)]]
SEXP rcpp_testint(
SEXP x, SEXP y
) {
const int *px = INTEGER_RO(x);
const int *py = INTEGER_RO(y);
R_xlen_t n = Rf_xlength(x);
SEXP out = PROTECT(Rf_allocVector(REALSXP, n));
double *pout;
pout = REAL(out);
for(R_xlen_t i = 0; i < n; ++i) {
pout[i] = px[i] + py[i];
}
UNPROTECT(1);
return out;
}
"
Rcpp::sourceCpp(code = code)
n <- 1e7
x <- sample(1:n)
y <- sample(1:n)
gc()
foo <- bench::mark(
testint = rcpp_testint(x, y),
testdbl = rcpp_testdbl(x, y),
check = FALSE,
min_iterations = 100,
)
summary(foo)
ggplot2::autoplot(foo)
f <- ~ x + y / z
as.list(f)
get_all_vars(f)
vars(f)
all.vars(f)
terms(f)
as.list(f)[[2]]
as.list(f[[2]])
deparse(f)
f <- ~ `x` + y /z
deparse(f)
deparse(f, backtick = TRUE)
deparse(f, backtick = FALSE)
f
f <-  `x/y` + y /z
f <-  ~ `x/y` + y/z
f
deparse()
deparse(f)
as.list(f)
terms(f)
formula.tools::op(f)
?formula
trms <- terms(f)
attr(trms, "variables")
vars <- attr(trms, "variables")
vars
vars[[1]]
vars[[1]][[1]]
class(vars)
all.vars(f)
x <- 1:10
class(x) <- "factor"
x
library(mutatomic)
class(x) <- "mutatomic"
x
x[1]
x[1] <- -1
x
as.character(x)
methods(class = "factor")
`[[.factor`
`[[<-.factor`
all.equal.factor
all.equal
all.equal.default
?all.equal
summary(x)
?ops
?Ops
Ops.factor
methods("complex")
methods("integer")
methods("Integer")
methods("Compelx")
methods("Complex")
methods("Ops")
Ops
showMethods(Ops)
library(mutatomic)
x <- mutatomic(1:10)
y <- 1L
x + y
x^y
mutatomic(x^y)
Ops.factor
Ops.difftime
Complex
methods(class = "factor")
initialize
?initialize
library(mutatomic)
Rcpp::cppFunction(
"
bool rcpp_maybe_shared(RObject x) {
bool MAYBE_SHARED(x);
return x;
}
"
)
z <- sample(1:10)
rcpp_maybe_shared(z)
rcpp_maybe_shared(base::letters)
40*60
f <- ~ x + y^2 * sin(z)
all.vars(f)
terms(f)
as.list(f)
as.list(f[[2]])
?formula
f <- ~ x + y^2 * sin(z) - `=`
all.vars(f)
rapply(f, \(x)x)
rapply(as.list(f), \(x)x)
rapply(as.list(f), \(x)as.list(x))
as.character(f)
deparse(f)
deparse(f[[2]])
ft <- loadNamespace("formula.tools")
ft$op(f)
ft$op(f[[2]])
?formula.tools::op
terms(f)
?formula
labels(f\)
labels(f)
model.frame()
model.frame(
model.frame
model.frame.default
names(f)
?formula.tools
??formula.tools
library(formula.tools)
?formula.tools
library(formula.tools)
library(formula.tools)
??formula.tools
?formula.tools::formula.parts
rhs(f)
rhs.vars(f)
rhs(f)
op(f)
op.type(f)
terms(f)
as.list(f)
as.list(f[[2]])
all.vars(f)
library(collapse)
?collapse
?collapse::`AA4-collapse-options`
?collapse::`A0-collapse-documentation`
?collapse::set_collapse
collapse::set_collapse
TRUE | 0.1
TRUE & 0.1
as.logical(0.1)
?Ops
Ops
Ops.data.frame
setwd("D:/D Desktop/Publications/R package broadcast/broadcast/inst/tinytest/count")
# count number of tests
library(broadcast)
library(tinytest)
# set working directory to source file location
SourceFileLocation <- function() {
# BATCH way:
path <- funr::get_script_path()
if(!is.null(path)) return(path)
# R-Studio way:
if(Sys.getenv("RSTUDIO") == "1") {
if(rstudioapi::isAvailable(version_needed = NULL,child_ok = FALSE)) {
return(dirname(rstudioapi::getSourceEditorContext()$path))
}
if(is.null(knitr::current_input(dir = TRUE)) == FALSE) {
return(knitr::current_input(dir = TRUE))
}
return(getwd())
}
}
enumerate_total <- 0
# root folder tests ====
wd <- SourceFileLocation()
setwd(wd)
setwd("..")
getwd()
files <- list.files(normalizePath(getwd()), pattern = ".R", full.names = TRUE)
max.width <- max(stringi::stri_width(basename(files))) + 8
for(iFile in files) {
capture.output(source(normalizePath(iFile)), file = nullfile()) |> suppressMessages()
cat(stringi::stri_pad_right(basename(iFile), max.width), " -> ", enumerate,  "\n")
enumerate_total <- enumerate_total + enumerate
}
rem <-  setdiff(ls(), c("SourceFileLocation", "enumerate", "enumerate_total"))
rm(list = rem)
# sub folder tests ====
subfolders <- list.dirs() |> basename() |> setdiff(c(".", "count"))
wd <- SourceFileLocation()
setwd(wd)
setwd("..")
path <- file.path(getwd(), subfolders) |> normalizePath()
files <- list.files(path, pattern = "*.R", full.names = TRUE)
max.width <- max(stringi::stri_width(basename(files))) + 8
for(iSubFolder in subfolders) {
wd <- SourceFileLocation()
setwd(wd)
setwd("..")
setwd(normalizePath(iSubFolder))
getwd()
files <- list.files(normalizePath(getwd()), pattern = ".R", full.names = TRUE)
print(iSubFolder)
for(iFile in files) {
capture.output(source(normalizePath(iFile)), file = nullfile()) |> suppressMessages()
cat(stringi::stri_pad_right(basename(iFile), max.width), " -> ", enumerate,  "\n")
enumerate_total <- enumerate_total + enumerate
}
rem <-  setdiff(ls(), c("SourceFileLocation", "enumerate", "enumerate_total", "max.width"))
rm(list = rem)
cat("\n")
}
print(enumerate_total)
# end ====
