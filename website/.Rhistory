:
`:`  <- function(x, y) c(x, list(y))
:
`:`
1:10:1
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
#' @keywords internal
#' @noRd
.internal_check_dots <- function(dots.list, abortcall) {
# this check will not take much performance
if(length(dots.list) > 0L) {
if(!is.null(names(dots.list))) {
error.txt <- paste0(
"unknown arguments given:",
"\n",
paste(names(dots.list), collapse = ", ")
)
}
else {
error.txt <- paste0(
"unknown arguments given (unnamed)"
)
}
stop(simpleError(error.txt, call = abortcall))
}
}
#' @rdname size
#' @export
s <- function(x, ...) {
UseMethod("s", x)
}
#' @rdname size
#' @export
s.default <- function(x, m = if(is.null(dim(x))) 0L else dim(x), ...) {
.internal_check_dots(list(...), sys.call())
if(!is.numeric(m)) {
stop("`m` must be an integer")
}
if(length(m) > 1L && any(m == 0L)) {
stop("multi-element `m` cannot contain 0")
}
if(any(m < 0)) {
stop("`m` cannot be negative")
}
if(m[1L] == 0L) {
if(is.data.frame(x)) {
stop("`m = 0` not applicable for data.frames")
}
return(length(x))
}
else if(m > 0L) {
return(dim(x)[m])
}
else {
stop("m cannot be negative")
}
}
.with_array <- function(x, keywords, form, abortcall) {
if(length(form) != 2L) {
stop(simpleError("invalid formula given", call = abortcall))
}
env <- environment(form)
txt <- as.character(form)[2L]
out <- eval(parse(text = txt), keywords, enclos = env)
environment(form) <- NULL
return(out)
}
.formula2seq <- function(x, m, form, .abortcall) {
keywords <- list(
`:`  <- function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
return(.with_array(x, keywords, form, .abortcall))
}
x <- 1:10
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
keywords <- list(
`:`  <- function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
.with_array <- function(x, keywords, form, abortcall) {
if(length(form) != 2L) {
stop(simpleError("invalid formula given", call = abortcall))
}
env <- environment(form)
txt <- as.character(form)[2L]
out <- eval(parse(text = txt), env, enclos = keywords)
environment(form) <- NULL
return(out)
}
.formula2seq <- function(x, m, form, .abortcall) {
keywords <- list(
`:`  <- function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
return(.with_array(x, keywords, form, .abortcall))
}
x <- 1:10
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
#' @keywords internal
#' @noRd
.internal_check_dots <- function(dots.list, abortcall) {
# this check will not take much performance
if(length(dots.list) > 0L) {
if(!is.null(names(dots.list))) {
error.txt <- paste0(
"unknown arguments given:",
"\n",
paste(names(dots.list), collapse = ", ")
)
}
else {
error.txt <- paste0(
"unknown arguments given (unnamed)"
)
}
stop(simpleError(error.txt, call = abortcall))
}
}
#' @rdname size
#' @export
s <- function(x, ...) {
UseMethod("s", x)
}
#' @rdname size
#' @export
s.default <- function(x, m = if(is.null(dim(x))) 0L else dim(x), ...) {
.internal_check_dots(list(...), sys.call())
if(!is.numeric(m)) {
stop("`m` must be an integer")
}
if(length(m) > 1L && any(m == 0L)) {
stop("multi-element `m` cannot contain 0")
}
if(any(m < 0)) {
stop("`m` cannot be negative")
}
if(m[1L] == 0L) {
if(is.data.frame(x)) {
stop("`m = 0` not applicable for data.frames")
}
return(length(x))
}
else if(m > 0L) {
return(dim(x)[m])
}
else {
stop("m cannot be negative")
}
}
.with_array <- function(x, keywords, form, abortcall) {
if(length(form) != 2L) {
stop(simpleError("invalid formula given", call = abortcall))
}
env <- environment(form)
txt <- as.character(form)[2L]
out <- eval(parse(text = txt), keywords, enclos = env)
environment(form) <- NULL
return(out)
}
.formula2seq <- function(x, m, form, .abortcall) {
keywords <- list(
`:`  <- function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
return(.with_array(x, keywords, form, .abortcall))
}
x <- 1:10
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
#' @keywords internal
#' @noRd
.internal_check_dots <- function(dots.list, abortcall) {
# this check will not take much performance
if(length(dots.list) > 0L) {
if(!is.null(names(dots.list))) {
error.txt <- paste0(
"unknown arguments given:",
"\n",
paste(names(dots.list), collapse = ", ")
)
}
else {
error.txt <- paste0(
"unknown arguments given (unnamed)"
)
}
stop(simpleError(error.txt, call = abortcall))
}
}
#' @rdname size
#' @export
s <- function(x, ...) {
UseMethod("s", x)
}
#' @rdname size
#' @export
s.default <- function(x, m = if(is.null(dim(x))) 0L else dim(x), ...) {
.internal_check_dots(list(...), sys.call())
if(!is.numeric(m)) {
stop("`m` must be an integer")
}
if(length(m) > 1L && any(m == 0L)) {
stop("multi-element `m` cannot contain 0")
}
if(any(m < 0)) {
stop("`m` cannot be negative")
}
if(m[1L] == 0L) {
if(is.data.frame(x)) {
stop("`m = 0` not applicable for data.frames")
}
return(length(x))
}
else if(m > 0L) {
return(dim(x)[m])
}
else {
stop("m cannot be negative")
}
}
.with_array <- function(x, keywords, form, abortcall) {
if(length(form) != 2L) {
stop(simpleError("invalid formula given", call = abortcall))
}
env <- environment(form)
txt <- as.character(form)[2L]
out <- eval(parse(text = txt), keywords, enclos = env)
environment(form) <- NULL
return(out)
}
.formula2seq <- function(x, m, form, .abortcall) {
keywords <- list(
`:`  <- function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
return(.with_array(x, keywords, form, .abortcall))
}
x <- 1:10
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
?eval
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
.formula2seq <- function(x, m, form, .abortcall) {
keywords <- list(
`:` = function(x, y) c(x, list(y)),
.M = m,
.N = s(x, m)
)
return(.with_array(x, keywords, form, .abortcall))
}
.formula2seq(x, 0L, ~ 1:.N:1, sys.call())
130*18
130*18*121
130*18*1.21
130*18*1.25
130*18*1.21
130*6.50
130*6.50*1.25
statmod::qres.invgauss
?pinvgauss
statmod::pinvgauss
?pwald
130*12.5*1.25
130*12.5*1.21
lamba <- 5
n <- 1000
x <- rpois(n, lambda)
lambda = 5
x <- rpois(n, lambda)
ppois(6, lamba)
myecdf <- ecdf(x)
ecdf(5)
myecdf(5)
myecdf(6)
n <- 1e6
x <- rpois(n, lambda)
ppois(6, lambda)
myecdf <- ecdf(x)
myecdf(6)
mean(x <= 6)
sim <- matrix(rnorm(1e6 * 1e3), 1e6, 1e3)
18*18
18*118
install.packages("ordbetareg")
?ordbetareg::dordbeta()
44.15*1.21
sim <- matrix(rnorm(1000*500), 1000, 500)
sim <- matrix(rnorm(1000*5000), 1000, 5000)
sim2 <- as.data.frame(sim)
y <- rnorm(1000)
library(broadcast)
sim <- matrix(rnorm(1000*5000), 1000, 5000)
sim2 <- as.data.frame(sim)
y <- rnorm(1000)
foo <- bench::mark(
ecp_mat = ecumprob(y, sim),
ecp_df = ecumprob(y, sim2),
ecp_conv = ecumprob(y, as.matrix(sim2)),
min_iterations = 500
)
summary(foo)
plot(foo)
foo <- bench::mark(
ecp_mat = ecumprob(y, sim),
ecp_df = ecumprob(y, sim2),
# ecp_conv = ecumprob(y, as.matrix(sim2)),
min_iterations = 500
)
summary(foo)
plot(foo)
install.packages("installr")
installr::updateR()
tinycodet::aes_pro()
?tinycodet::aes_pro()
/
90/4
10*130
10*130 /60
10*130 /60 /4
5^(1/3) * 1000
5000^(1/3)
library(tinycodet)
foo <- bench::mark(
baseR = `%inr%` <- imager::`%inr%`,
tinycodet = import_inops(expose = "imager"),
min_iterations = 500
)
library(tinycodet)
foo <- bench::mark(
baseR = `%inr%` <- imager::`%inr%`,
tinycodet = import_inops(expose = "imager"),
min_iterations = 100
)
summary(foo)
foo <- bench::mark(
baseR = imager::`%inr%`,
# tinycodet = import_inops(expose = "imager"),
min_iterations = 100
)
foo <- bench::mark(
# baseR = imager::`%inr%`,
tinycodet = import_inops(expose = "imager"),
min_iterations = 100
)
warnings()
foo <- bench::mark(
baseR = imager::`%inr%`,
tinycodet = import_inops(expose = "imager"),
min_iterations = 10
)
foo <- bench::mark(
baseR = imager::`%inr%`,
tinycodet = import_inops(expose = "imager"),
min_iterations = 2
)
25*5*6
30*5*6
40*5*6
35*5*6
setwd("D:/D Desktop/Publications/R package broadcast/broadcast")
# set-up ====
library(stringi)
source("siteutils.R")
################################################################################
# Create man pages ====
#
# make vector of function and topic names ====
funs <- c(
getNamespaceExports("broadcast"),
"broadcast_help", "broadcast_operators", "broadcast_casting",
"broadcast_bind", "broadcast_linearalgebra", "broadcast_typecast"
)
## pre-process Rds for linking ====
lst.files <- list.files("man", pattern = "Rd")
pkgpath <- getwd()
for(i in lst.files) {
print(i)
filepath <- file.path("man", i)
outpath <- file.path("preprocess", "man")
rd_links(filepath, outpath, funs)
}
## convert Rd to qmd ====
lst.files <- list.files(file.path("preprocess", "man"), pattern = "Rd")
pkgpath <- getwd()
for(i in lst.files) {
print(i)
filepath <- file.path("preprocess", "man", i)
temp_html <- stri_replace_last(i, ".html", fixed = ".Rd")
temp_html <- file.path("preprocess", "man", temp_html)
outpath <- file.path("website", "man")
rd2qmd(filepath, temp_html, outpath, pkgpath)
}
## adapt man titles ====
format <- "
format:
html:
css: /man.css
"
detection <- "---\ntitle:"
lst.files <- list.files("website/man/", pattern = "qmd")
for(i in lst.files) {
print(i)
filename <- i
title <- stri_replace_last(filename, "", fixed = ".qmd")
temp <- readLines(file.path("website", "man", filename))
check <- stringi::stri_detect(paste0(temp[1:2], collapse = "\n"), fixed = detection)
if(!check && !stri_detect(title, fixed = "aaa")) {
temp <- c("---", paste0("title: ", title), format, "---", temp)
writeLines(temp, file.path("website", "man", filename))
}
}
## unpack links in man pages ====
lst.files <- list.files("website/man/", pattern = "qmd")
for(i in lst.files) {
filepath <- file.path("website", "man", i)
outpath <- file.path("website", "man")
qmd_extractlinks(filepath, outpath, funs)
}
################################################################################
# Process Index page ====
#
# clear any present links ====
funs <- getNamespaceExports("broadcast")
filepath <- file.path("website", "index.qmd")
temp <- readLines(filepath)
p <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
rp <- paste0("`", funs, "()`")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "index.qmd"))
# # create links ====
p <- paste0("`", funs, "()`")
rp <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "index.qmd"))
################################################################################
# Process Vignettes, Index page ====
#
# clear any present links in vignettes ====
funs <- getNamespaceExports("broadcast")
lst.files <- list.files(file.path("website", "vignettes"), pattern = "qmd")
for(i in lst.files) {
print(i)
filepath <- file.path("website", "vignettes", i)
temp <- readLines(filepath)
p <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
rp <- paste0("`", funs, "()`")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "vignettes", i))
}
# create links in vignettes ====
funs <- getNamespaceExports("broadcast")
lst.files <- list.files(file.path("website", "vignettes"), pattern = "qmd")
for(i in lst.files) {
print(i)
filepath <- file.path("website", "vignettes", i)
temp <- readLines(filepath)
p <- paste0("`", funs, "()`")
rp <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "vignettes", i))
}
################################################################################
# Process About pages ====
#
# clear any present links in About pages ====
funs <- getNamespaceExports("broadcast")
lst.files <- list.files(file.path("website", "About"), pattern = "qmd")
for(i in lst.files) {
print(i)
filepath <- file.path("website", "About", i)
temp <- readLines(filepath)
p <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
rp <- paste0("`", funs, "()`")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "About", i))
}
# create links in About pages ====
funs <- getNamespaceExports("broadcast")
lst.files <- list.files(file.path("website", "About"), pattern = "qmd")
for(i in lst.files) {
print(i)
filepath <- file.path("website", "About", i)
temp <- readLines(filepath)
p <- paste0("`", funs, "()`")
rp <- paste0("[", funs, "()]", "(/man/", rd_index(funs), ".qmd)")
temp <- stri_replace_all(
temp, rp, fixed = p, vectorize_all = FALSE
)
writeLines(temp, file.path("website", "About", i))
}
# end of rd2qmd ====
setwd("D:/D Desktop/Publications/R package broadcast/broadcast/website")
library(quarto)
# always render first
quarto_render()
# wait preview until render is done
quarto_preview()
quarto::quarto_add_extension("fontawesome")
