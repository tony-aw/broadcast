int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_prod)]]
double rcpp_mergedims_prod(
x, int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_is_broadcastable)]]
SEXP rcpp_mergedims_is_broadcastable(
SEXP x_dim
) {
int n = Rf_length(x_dim);
SEXP out = PROTECT(Rf_allocVector(LGLSXP, n));
int *pout = LOGICAL(out);
const int *px_dim = INTEGER_RO(x_dim);
for(int i = 0; i < n; ++i) {
if(px_dim[i] == 1) {
pout[i] = 1;
}
else {
pout[i] = 0;
}
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_findfirst_mergable_dims)]]
SEXP rcpp_mergedims_findfirst_mergable_dims(
SEXP Xbroadcastable, SEXP Ybroadcastable
) {
int n = Rf_length(Xbroadcastable);
const int *pXbroadcastable = INTEGER_RO(Xbroadcastable);
const int *pYbroadcastable = INTEGER_RO(Ybroadcastable);
SEXP startend = PROTECT(Rf_allocVector(INTSXP, 2));
int *pstartend = INTEGER(startend);
pstartend[0] = 0;
pstartend[1] = 0;
int found = 0;
int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_prod)]]
double rcpp_mergedims_prod(
SEXP x, int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_is_broadcastable)]]
SEXP rcpp_mergedims_is_broadcastable(
SEXP x_dim
) {
int n = Rf_length(x_dim);
SEXP out = PROTECT(Rf_allocVector(LGLSXP, n));
int *pout = LOGICAL(out);
const int *px_dim = INTEGER_RO(x_dim);
for(int i = 0; i < n; ++i) {
if(px_dim[i] == 1) {
pout[i] = 1;
}
else {
pout[i] = 0;
}
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_findfirst_mergable_dims)]]
SEXP rcpp_mergedims_findfirst_mergable_dims(
SEXP Xbroadcastable, SEXP Ybroadcastable
) {
int n = Rf_length(Xbroadcastable);
const int *pXbroadcastable = INTEGER_RO(Xbroadcastable);
const int *pYbroadcastable = INTEGER_RO(Ybroadcastable);
SEXP startend = PROTECT(Rf_allocVector(INTSXP, 2));
int *pstartend = INTEGER(startend);
pstartend[0] = 0;
pstartend[1] = 0;
int found = 0;
int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
"
Rcpp::sourceCpp(code = code)
x.dim <- 1:10
y.dim <- 10:1
xbroadcastable <- rcpp_mergedims_is_broadcastable(x.dim)
ybroadcastable <- rcpp_mergedims_is_broadcastable(y.dim)
rcpp_mergedims_findfirst_mergable_dims(xbroadcastable, ybroadcastable)
rcpp_mergedims_rng(1L, 10L)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_is_broadcastable)]]
SEXP rcpp_mergedims_is_broadcastable(
SEXP x_dim
) {
int n = Rf_length(x_dim);
SEXP out = PROTECT(Rf_allocVector(LGLSXP, n));
int *pout = LOGICAL(out);
const int *px_dim = INTEGER_RO(x_dim);
for(int i = 0; i < n; ++i) {
if(px_dim[i] == 1) {
pout[i] = 1;
}
else {
pout[i] = 0;
}
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_findfirst_mergable_dims)]]
SEXP rcpp_mergedims_findfirst_mergable_dims(
SEXP Xbroadcastable, SEXP Ybroadcastable
) {
int n = Rf_length(Xbroadcastable);
const int *pXbroadcastable = INTEGER_RO(Xbroadcastable);
const int *pYbroadcastable = INTEGER_RO(Ybroadcastable);
SEXP startend = PROTECT(Rf_allocVector(INTSXP, 2));
int *pstartend = INTEGER(startend);
pstartend[0] = 0;
pstartend[1] = 0;
int found = 0;
int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_prod)]]
double rcpp_mergedims_prod(
SEXP x, int start, int end
) {
int n = end - start + 1;
double out = 1;
for(int i = (start - 1); i < end; ++i) {
out *= x[i];
}
return out;
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_is_broadcastable)]]
SEXP rcpp_mergedims_is_broadcastable(
SEXP x_dim
) {
int n = Rf_length(x_dim);
SEXP out = PROTECT(Rf_allocVector(LGLSXP, n));
int *pout = LOGICAL(out);
const int *px_dim = INTEGER_RO(x_dim);
for(int i = 0; i < n; ++i) {
if(px_dim[i] == 1) {
pout[i] = 1;
}
else {
pout[i] = 0;
}
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_findfirst_mergable_dims)]]
SEXP rcpp_mergedims_findfirst_mergable_dims(
SEXP Xbroadcastable, SEXP Ybroadcastable
) {
int n = Rf_length(Xbroadcastable);
const int *pXbroadcastable = INTEGER_RO(Xbroadcastable);
const int *pYbroadcastable = INTEGER_RO(Ybroadcastable);
SEXP startend = PROTECT(Rf_allocVector(INTSXP, 2));
int *pstartend = INTEGER(startend);
pstartend[0] = 0;
pstartend[1] = 0;
int found = 0;
int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_prod)]]
double rcpp_mergedims_prod(
SEXP x, int start, int end
) {
int n = end - start + 1;
double out = 1;
const int *px - INTEGER_RO(x);
for(int i = (start - 1); i < end; ++i) {
out *= px[i];
}
return out;
}
"
Rcpp::sourceCpp(code = code)
code <- "
#include <Rcpp.h>
using namespace Rcpp;
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_is_broadcastable)]]
SEXP rcpp_mergedims_is_broadcastable(
SEXP x_dim
) {
int n = Rf_length(x_dim);
SEXP out = PROTECT(Rf_allocVector(LGLSXP, n));
int *pout = LOGICAL(out);
const int *px_dim = INTEGER_RO(x_dim);
for(int i = 0; i < n; ++i) {
if(px_dim[i] == 1) {
pout[i] = 1;
}
else {
pout[i] = 0;
}
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_findfirst_mergable_dims)]]
SEXP rcpp_mergedims_findfirst_mergable_dims(
SEXP Xbroadcastable, SEXP Ybroadcastable
) {
int n = Rf_length(Xbroadcastable);
const int *pXbroadcastable = INTEGER_RO(Xbroadcastable);
const int *pYbroadcastable = INTEGER_RO(Ybroadcastable);
SEXP startend = PROTECT(Rf_allocVector(INTSXP, 2));
int *pstartend = INTEGER(startend);
pstartend[0] = 0;
pstartend[1] = 0;
int found = 0;
int idx;
for(int i = 1; i < n; ++i) {
idx = i + 1;
if(found == 0 && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[0] = idx - 1;
found = 1;
}
if((found > 0) && pXbroadcastable[i] == pXbroadcastable[i-1] && pYbroadcastable[i] == pYbroadcastable[i -1]) {
pstartend[1] = idx;
}
if(found > 0) {
if(pXbroadcastable[i] != pXbroadcastable[i-1] || pYbroadcastable[i] != pYbroadcastable[i -1]) {
UNPROTECT(1);
return(startend);
}
}
}
UNPROTECT(1);
return(startend);
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_rng)]]
SEXP rcpp_mergedims_rng(
int start, int end
) {
int n = end - start + 1;
SEXP out = PROTECT(Rf_allocVector(INTSXP, n));
int *pout = INTEGER(out);
for(int i = 0; i < n; ++i) {
pout[i] = start + i;
}
UNPROTECT(1);
return out;
}
//' @keywords internal
//' @noRd
// [[Rcpp::export(rcpp_mergedims_prod)]]
double rcpp_mergedims_prod(
SEXP x, int start, int end
) {
int n = end - start + 1;
double out = 1;
const int *px = INTEGER_RO(x);
for(int i = (start - 1); i < end; ++i) {
out *= px[i];
}
return out;
}
"
Rcpp::sourceCpp(code = code)
x.dim <- 1:10
y.dim <- 10:1
xbroadcastable <- rcpp_mergedims_is_broadcastable(x.dim)
ybroadcastable <- rcpp_mergedims_is_broadcastable(y.dim)
rcpp_mergedims_findfirst_mergable_dims(xbroadcastable, ybroadcastable)
rcpp_mergedims_rng(1L, 10L)
rcpp_mergedims_prod(1:10)
rcpp_mergedims_prod(1:10, 2, 9)
prod(2:9)
x <- sample(1:100, 10)
rcpp_mergedims_prod(x, 2, 9)
prod(x[2:9])
rcpp_mergedims_prod(x, 2, 9) == prod(x[2:9])
setwd("D:/D Desktop/Publications/R package broadcast/broadcast/website")
library(quarto)
quarto_render()
# wait until render is done!
quarto_preview()
setwd("D:/D Desktop/Publications/R package broadcast/broadcast/website")
library(quarto)
quarto_render()
# wait until render is done!
quarto_preview()
